version: '3'

services:
  backend:
    image: relatives_backend:latest
    build:
      context: .
      dockerfile: src/backend/docker/Dockerfile
    env_file:
      - .env
    volumes:
      - ./src/backend/relatives.db:/app/relatives.db
    ports:
      - "8000:8000"
